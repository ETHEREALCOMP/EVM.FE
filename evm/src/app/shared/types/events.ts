export interface createEvent {
    title: string;
    description: string;
    location: string;
}

export type EventType = {
    id: string;
    name: string;
    description: string;
    location: string;
};