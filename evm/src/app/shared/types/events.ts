export interface createEvent {
    title: string;
    description: string;
    location: string;
}

export type EventType = {
    id: string;
    title: string;
    description: string;
    location: string;
};